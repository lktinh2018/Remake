void initBuzzer() {
  pinMode(3, OUTPUT);
}

void alarmClock(int h, int m, int s, int repeat) {
  ;
//  if(hour==h && minute==m && second==s) {
//      printToLCD = new WorkScheduler(3000UL, &updateLCD);
//  } else
}

